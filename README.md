🧑‍💻 User Management App – Vanilla JavaScript
📌 Description

A frontend application built using HTML, CSS, and Vanilla JavaScript that fetches user data from an API, allows searching users by name, and lets users add personal notes that persist using localStorage.

🚀 Features

Fetches user data from a public API

Displays users dynamically on the UI

Search users by name (case-insensitive)

Add notes/tags to individual users

Persist notes using localStorage

Restore user data and notes on page refresh

🛠️ Tech Stack

HTML

CSS

JavaScript (Vanilla)

Fetch API

Browser localStorage

🧠 Key Learnings

DOM manipulation and event handling

Asynchronous API calls using async/await

State management using JavaScript arrays and objects

Data persistence using localStorage

UI re-rendering based on filtered data

Debugging real-world frontend issues

🔄 How It Works

User data is fetched from an API on page load

Data is stored in localStorage

UI is rendered from stored data

Search filters the user list dynamically

Notes are saved per user and persist after refresh
